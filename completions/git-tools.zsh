#compdef git-repo-manager git-stats

# AUTOMATICALLY GENERATED by `shtab` via generate_completions.py

typeset -A opt_args

_shtab_git_repo_manager_commands() {
  local _commands=(
    "scan:"
    "status:"
  )
  _describe 'git-repo-manager commands' _commands
}

_shtab_git_repo_manager_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)"{-V,--version}"[show program\'s version number and exit]"
  {-c,--config}"[Path to the configuration file (default\: \~\/.config\/git-tools\/git-repo-manager).]:config:"
)

# guard to ensure default positional specs are added only once per session
_shtab_git_repo_manager_defaults_added=0

_shtab_git_repo_manager_scan_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_git_repo_manager_scan_defaults_added=0

_shtab_git_repo_manager_status_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--fetch[Fetch from remote before checking status (default\: origin). (default\: None)]:fetch:"
  {-j,--jobs}"[Number of parallel jobs to use (default\: CPU count). (default\: None)]:jobs:"
  "--storage[Show number of pack files and loose objects. (default\: False)]"
)

# guard to ensure default positional specs are added only once per session
_shtab_git_repo_manager_status_defaults_added=0


_shtab_git_repo_manager() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*' default='*::: :->git-repo-manager'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_git_repo_manager_defaults_added )); then
    if (( ${_shtab_git_repo_manager_options[(I)${(q)one_or_more}*]} +          ${_shtab_git_repo_manager_options[(I)${(q)remainder}*]} +          ${_shtab_git_repo_manager_options[(I)${(q)default}]} == 0 )); then
      _shtab_git_repo_manager_options+=(': :_shtab_git_repo_manager_commands' '*::: :->git-repo-manager')
    fi
    _shtab_git_repo_manager_defaults_added=1
  fi
  _arguments -C -s $_shtab_git_repo_manager_options

  case $state in
    git-repo-manager)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_git_repo_manager-$line[1]:"
      case $line[1] in
        scan) _arguments -C -s $_shtab_git_repo_manager_scan_options ;;
        status) _arguments -C -s $_shtab_git_repo_manager_status_options ;;
      esac
  esac
}

_shtab_git_stats_commands() {
  local _commands=(
    "commit:Lists all commits with file change counts (Regular vs Symlinks)."
    "pack:List git pack files and loose objects."
  )
  _describe 'git-stats commands' _commands
}

_shtab_git_stats_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)"{-V,--version}"[Show the version and exit.]"
)

# guard to ensure default positional specs are added only once per session
_shtab_git_stats_defaults_added=0

_shtab_git_stats_commit_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  ":Path to the git repository.:_files -/"
)

# guard to ensure default positional specs are added only once per session
_shtab_git_stats_commit_defaults_added=0

_shtab_git_stats_pack_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  {-H,--human}"[Display human-readable sizes (e.g., KiB, MiB).]"
  "--loose-uncompressed[Include uncompressed size for loose objects (can be slow).]"
  "--actual-size[Include the actual full uncompressed size of all objects (can be slow).]"
  {-v,--verbose}"[Print how long time it took to obtain each set of data.]"
  ":Path to the git repository.:_files -/"
)

# guard to ensure default positional specs are added only once per session
_shtab_git_stats_pack_defaults_added=0


_shtab_git_stats() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*' default='*::: :->git-stats'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_git_stats_defaults_added )); then
    if (( ${_shtab_git_stats_options[(I)${(q)one_or_more}*]} +          ${_shtab_git_stats_options[(I)${(q)remainder}*]} +          ${_shtab_git_stats_options[(I)${(q)default}]} == 0 )); then
      _shtab_git_stats_options+=(': :_shtab_git_stats_commands' '*::: :->git-stats')
    fi
    _shtab_git_stats_defaults_added=1
  fi
  _arguments -C -s $_shtab_git_stats_options

  case $state in
    git-stats)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_git_stats-$line[1]:"
      case $line[1] in
        commit) _arguments -C -s $_shtab_git_stats_commit_options ;;
        pack) _arguments -C -s $_shtab_git_stats_pack_options ;;
      esac
  esac
}

_git_tools_handler() {
  local service=${service:-${words[1]:t}}
  case $service in
    git-repo-manager) _shtab_git_repo_manager "$@" ;;
    git-stats) _shtab_git_stats "$@" ;;
  esac
}

if [[ $zsh_eval_context[-1] == eval ]]; then
  compdef _git_tools_handler git-repo-manager
  compdef _git_tools_handler git-stats
else
  _git_tools_handler "$@"
fi
